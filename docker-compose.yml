services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - one_metal_data:/app/storage
    restart: unless-stopped
volumes:
  one_metal_data:
